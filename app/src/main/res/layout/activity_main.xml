<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".MainActivity"
    tools:showIn="@layout/activity_main">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:id="@+id/getordertracker"
        android:hint="Enter Tracking code"
        android:textSize="16sp"
        android:paddingBottom="20dp"
        android:layout_marginTop="20dp"/>

    <Button
        android:layout_width="match_parent"
        android:text="check"
        android:layout_below="@+id/getordertracker"
        android:id="@+id/btnchecktrack"
        android:layout_height="60dp" />


    <TextView
        android:id="@+id/idview"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/btnchecktrack"
        android:textColor="@color/colorPrimary"
        android:textSize="16sp" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:id="@+id/verticalTimeLineButton"
        android:layout_marginTop="60dp"
        android:layout_below="@id/idview"
        android:text="@string/vertical_timeline"
        android:textSize="16sp"
        android:textAllCaps="false" />



</RelativeLayout>